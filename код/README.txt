# Telegram Phone Shop Mini App

Простое мини-приложение Telegram для продажи телефонов 📱

## 🚀 Запуск локально

1. Установи зависимости:
   ```
   pip install -r requirements.txt
   ```

2. Запусти Flask сервер:
   ```
   python webapp.py
   ```

3. В отдельном терминале запусти Telegram-бота:
   ```
   python bot.py
   ```

4. Замени в `bot.py`:
   ```
   TOKEN = "ВСТАВЬ_СВОЙ_ТОКЕН_БОТА"
   WEBAPP_URL = "http://localhost:5000/catalog"
   ```

5. Напиши своему боту в Telegram `/start` и открой каталог.

## 🌍 Деплой на Render (бесплатно)

1. Зайди на https://render.com
2. Создай новый Web Service
3. Загрузить этот проект как репозиторий
4. Команда запуска: `python webapp.py`
5. После деплоя вставь ссылку в `WEBAPP_URL` в боте.
